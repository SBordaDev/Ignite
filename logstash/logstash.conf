input {
  tcp {
    port => 50000
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "ignite-logs-%{+YYYY.MM.dd}"
  }
  # Esta línea imprimirá los logs en la consola de Docker para confirmar que llegan
  stdout { codec => rubydebug }
}